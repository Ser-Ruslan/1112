# 🏥 Heart Disease - Heart Rate Prediction

**Веб-приложение на Gradio для предсказания максимального пульса при физической нагрузке**

---

## 📋 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Установка на другом ПК](#-установка-на-другом-пк)
3. [Описание проекта](#-описание-проекта)
4. [Архитектура и структура](#-архитектура-и-структура)
5. [Модель и результаты](#-модель-и-результаты)
6. [Тестирование](#-тестирование)
7. [Использование API](#-использование-api)

---

## 🚀 Быстрый старт

**На Windows с PowerShell:**

```powershell
# 1. Активировать виртуальное окружение
.\.venv\Scripts\Activate.ps1

# 2. Запустить приложение
python -m app.app

# 3. Открыть в браузере
# Перейдите на: http://127.0.0.1:7861
```

**На Linux/Mac:**

```bash
# 1. Активировать окружение
source .venv/bin/activate

# 2. Запустить приложение
python -m app.app

# 3. Открыть в браузере
# Перейдите на: http://127.0.0.1:7861
```

---

## 💻 Установка на другом ПК

### Требования
- **Windows 7+, macOS 10.14+, Linux (любой дистрибутив)**
- **Python 3.8+** (рекомендуется 3.10 или 3.11)

### Пошаговая инструкция

#### Шаг 1: Установка Python

**Windows:**
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **отметьте галочку "Add Python to PATH"**
3. Нажмите "Install Now"

**macOS:**
```bash
brew install python3
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update && sudo apt install python3 python3-pip python3-venv
```

#### Шаг 2: Клонирование проекта

```bash
git clone https://github.com/Ser-Ruslan/1112.git
cd 1112
```

Или скачайте ZIP и распакуйте в папку.

#### Шаг 3: Создание виртуального окружения

```bash
python -m venv .venv
```

#### Шаг 4: Активация окружения

**Windows (PowerShell):**
```powershell
.\.venv\Scripts\Activate.ps1
```

**Windows (Command Prompt):**
```cmd
.venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source .venv/bin/activate
```

#### Шаг 5: Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Шаг 6: Запуск приложения

```bash
python -m app.app
```

Приложение откроется на `http://127.0.0.1:7861`

### Рекомендуемые VS Code расширения

1. **Python** (Microsoft) - код-комплит и отладка
2. **Pylance** - статический анализ Python
3. **Pylint** - проверка кода на ошибки
4. **Jupyter** - для интерактивного развития

**Установка расширений:**
- Нажмите `Ctrl+Shift+X` в VS Code
- Введите название расширения
- Нажмите "Install"

---

## 📊 Описание проекта

### Что это приложение делает?

Предсказывает **максимальный пульс (тахикардию)** при физической нагрузке на основе медицинских показателей пациента.

**Входные данные:**
- Возраст пациента
- Артериальное давление в покое
- Уровень холестерина
- ST депрессия (ЭКГ показатель)
- Пол, тип боли в груди, уровень глюкозы, ЭКГ в покое, стенокардия при нагрузке, наклон ST, тип таллассемии

**Выходные данные:**
- Предсказанный максимальный пульс (уд/мин)
- График: точки обучения + регрессионная линия
- Метрики модели (R², уравнение)

### Датасет

- **Источник:** Heart Disease Cleveland
- **Всего записей:** 301 пациент
- **Обучающая выборка:** 240 примеров
- **Тестовая выборка:** 61 пример
- **Целевая переменная:** thalach (максимальный пульс)

### Особенности

✅ Полностью на **русском языке**  
✅ Интерактивный веб-интерфейс (Gradio)  
✅ **Закреплены версии всех зависимостей**  
✅ 15 unit тестов (**100% прохождение**)  
✅ Сохраненная обученная модель  
✅ **Исправлены все warnings** о feature names  

---

## 📁 Архитектура и структура

```
1112/
├── app/
│   ├── __init__.py
│   ├── app.py                      ← Gradio интерфейс (206 строк)
│   └── model.py                    ← Загрузка модели, предсказания (176 строк)
│
├── models/
│   ├── regression_model.joblib     ← Обученная LinearRegression модель
│   ├── regression_scaler.joblib    ← StandardScaler для нормализации
│   └── regression_config.json      ← Коэффициенты, метрики, признаки
│
├── tests/
│   ├── __init__.py
│   └── test_predict.py             ← 15 unit тестов (все ✅ PASSED)
│
├── cleve.mod                        ← Исходный датасет (CSV)
├── train_regression_model.py        ← Скрипт для обучения модели
├── requirements.txt                 ← Закрепленные версии зависимостей
└── README.md                        ← Этот файл
```

### Ключевые файлы

| Файл | Назначение | Строк |
|------|-----------|-------|
| `app/app.py` | Gradio веб-интерфейс со всеми компонентами | 206 |
| `app/model.py` | Класс RegressionModel, загрузка, предсказания | 176 |
| `tests/test_predict.py` | 15 unit тестов для проверки модели | ~400 |
| `train_regression_model.py` | Скрипт обучения (уже исполнен) | ~80 |
| `models/regression_model.joblib` | Сохраненная обученная модель | 0.7 KB |
| `models/regression_scaler.joblib` | Scaler для нормализации данных | 1.2 KB |

---

## 📈 Модель и результаты

### Параметры модели

| Параметр | Значение |
|----------|---------|
| **Алгоритм** | Linear Regression (scikit-learn) |
| **Входных признаков** | 11 (4 числовых + 7 категориальных) |
| **Размер обучающего набора** | 240 примеров |
| **Размер тестового набора** | 61 пример |

### Метрики производительности

| Метрика | Значение | Интерпретация |
|---------|---------|---------------|
| **R² Score** | **0.1884** | Модель объясняет ~19% вариации пульса |
| **RMSE** | **16.71 bpm** | Средняя ошибка предсказания |
| **MAE** | **13.87 bpm** | Средняя абсолютная ошибка |

### Уравнение регрессии

$$y = 149.94 - 9.00 \cdot age + 2.05 \cdot trestbps + 1.61 \cdot chol - 1.53 \cdot oldpeak - 0.42 \cdot sex - 0.72 \cdot cp + 0.61 \cdot fbs - 0.14 \cdot restecg - 6.47 \cdot exang + 5.31 \cdot slope + 0.17 \cdot thal$$

### Интерпретация коэффициентов

| Признак | Коэффициент | Смысл |
|---------|------------|-------|
| **age** | **-8.99** | 👑 **Главный фактор:** +1 год → -9 bpm |
| **exang** | -6.47 | Стенокардия при нагрузке → -6.5 bpm |
| **slope** | +5.31 | ST восходящий → +5.3 bpm |
| **chol** | +1.61 | Холестерин → +1.6 bpm на 10 мг/дл |
| **trestbps** | +2.05 | Давление → +2 bpm на 10 мм рт.ст. |

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Активировать окружение (если не активировано)
.venv\Scripts\Activate.ps1  # Windows
source .venv/bin/activate  # Linux/Mac

# Запустить все тесты
python -m pytest tests/test_predict.py -v
```

### Результат

```
========================= 15 passed in 0.79s ==========================

tests/test_predict.py::TestRegressionModel::test_model_loaded_successfully PASSED [ 6%]
tests/test_predict.py::TestRegressionModel::test_model_has_correct_config PASSED [ 13%]
tests/test_predict.py::TestRegressionModel::test_prediction_output_format PASSED [ 20%]
tests/test_predict.py::TestRegressionModel::test_prediction_in_reasonable_range PASSED [ 26%]
tests/test_predict.py::TestRegressionModel::test_prediction_varies_with_age PASSED [ 33%]
tests/test_predict.py::TestRegressionModel::test_prepare_features PASSED [ 40%]
tests/test_predict.py::TestRegressionModel::test_categorical_encoding PASSED [ 46%]
tests/test_predict.py::TestRegressionModel::test_reproducibility PASSED [ 53%]
tests/test_predict.py::TestRegressionModel::test_get_model_info PASSED [ 60%]
tests/test_predict.py::TestRegressionModel::test_get_equation_latex PASSED [ 66%]
tests/test_predict.py::TestRegressionModel::test_get_r2_score PASSED [ 73%]
tests/test_predict.py::TestModelConsistency::test_feature_columns_match_config PASSED [ 80%]
tests/test_predict.py::TestModelConsistency::test_numeric_features_in_features PASSED [ 86%]
tests/test_predict.py::TestModelConsistency::test_categorical_features_encoded PASSED [ 93%]
tests/test_predict.py::TestModelConsistency::test_coefficients_match_features PASSED [100%]
```

### Что тестируется

✅ Загрузка модели и конфигурации  
✅ Корректный формат и диапазон предсказаний (60-220 bpm)  
✅ Кодирование категориальных признаков  
✅ Воспроизводимость результатов  
✅ Генерация LaTeX уравнения  
✅ Консистентность модели с конфигурацией  

---

## 💻 Использование API

### Импорт модели

```python
from app.model import RegressionModel

# Загрузить модель
model = RegressionModel()

# Сделать предсказание
result = model.predict(
    age=55, 
    trestbps=120, 
    chol=200, 
    oldpeak=1.5,
    sex='male', 
    cp='asympt', 
    fbs='false', 
    restecg='norm', 
    exang='false', 
    slope='flat', 
    thal='norm'
)

print(f"Предсказанный пульс: {result['prediction']:.1f} bpm")
# Вывод: Предсказанный пульс: 155.0 bpm
```

### Получение информации о модели

```python
# Информация о модели
info = model.get_model_info()
print(f"R² Score: {info['test_r2']:.4f}")
print(f"RMSE: {info['test_rmse']:.4f}")
print(f"Главный признак: {info['most_important_feature']}")

# LaTeX уравнение
equation = model.get_equation_latex()
print(equation)
```

---

## 📦 Зависимости

Все версии **закреплены** в `requirements.txt`:

```
pandas==1.5.3
numpy==1.24.3
scikit-learn==1.3.0
joblib==1.3.1
gradio==4.0.0
matplotlib==3.7.1
```

**Почему закреплены версии?**
- Обеспечивают воспроизводимость результатов
- Избегают конфликтов между версиями
- Гарантируют совместимость кода

---

## 🛠️ Разработка

### Переобучение модели

Если нужно переобучить модель на новых данных:

```bash
python train_regression_model.py
```

Это создаст новые файлы:
- `models/regression_model.joblib`
- `models/regression_scaler.joblib`
- `models/regression_config.json`

### Добавление новых тестов

Добавляйте тесты в `tests/test_predict.py` и запускайте:

```bash
python -m pytest tests/test_predict.py -v
```

---

## 🛠️ Технологии

- **Python 3.8+**
- **Gradio 4.0+** - веб-интерфейс
- **Scikit-learn** - LinearRegression модель
- **Pandas, NumPy** - обработка данных
- **Matplotlib** - визуализация
- **Joblib** - сохранение моделей



