# Структура и описание файлов проекта

## 📁 Дерево проекта

```
1112/
├── app/
│   ├── __init__.py
│   ├── app.py
│   └── model.py
│
├── models/
│   ├── regression_model.joblib
│   ├── regression_scaler.joblib
│   ├── regression_config.json
│   └── (другие файлы для классификации - не используются)
│
├── tests/
│   ├── __init__.py
│   └── test_predict.py
│
├── .venv/                           # Виртуальное окружение Python
├── train_regression_model.py        # Скрипт для обучения модели
├── cleve.mod                        # Исходные данные
├── requirements.txt                 # Зависимости Python
├── README.md                        # Основная документация
├── USAGE_GUIDE.md                   # Руководство использования
├── COMPLETION_CHECKLIST.md          # Чек-лист выполнения
└── Untitled*.ipynb                  # Jupyter notebooks (черновики)
```

---

## 📄 Описание ключевых файлов

### 🎯 app/app.py
**Главное приложение Gradio**

```python
# Основные функции:
- load_training_data()              # Загрузка обучающей выборки
- predict_and_plot()                # Предсказание + визуализация
- create_interface()                # Создание Gradio интерфейса
```

**Содержит:**
- 11 полей ввода (4 числовых + 7 dropdown)
- Scatter plot с регрессионной линией
- Вывод R² score и LaTeX уравнения
- 3 примера для быстрого тестирования

**Запуск:**
```bash
python -m app.app
```

---

### 🔧 app/model.py
**Модуль для работы с моделью**

```python
class RegressionModel:
    def __init__()                   # Загрузка модели и scaler
    def predict()                    # Предсказание для новых данных
    def _prepare_features()          # Подготовка и кодирование признаков
    def get_model_info()             # Информация о модели
    def get_equation_latex()         # LaTeX уравнение регрессии
    def get_r2_score()               # R² score

def load_model():                    # Загрузить модель на старте
```

**Отвечает за:**
- Загрузку модели, scaler, конфигурации
- Кодирование категориальных признаков
- Скейлирование числовых признаков
- Формирование LaTeX уравнения

---

### 📊 models/regression_model.joblib
**Обученная модель LinearRegression**

Содержит:
- Коэффициенты регрессии (для каждого из 11 признаков)
- Intercept (константный член)
- Fit-трансформации для всех 11 признаков

Создана скриптом `train_regression_model.py`

---

### ⚙️ models/regression_scaler.joblib
**StandardScaler для нормализации признаков**

Содержит:
- Средние значения (mean) для каждого признака
- Стандартные отклонения (std) для каждого признака
- Fit-трансформации на тренировочной выборке

Использует формулу: `(x - mean) / std`

---

### 📋 models/regression_config.json
**Конфигурация модели и метрики**

```json
{
  "feature_columns": [
    "age", "trestbps", "chol", "oldpeak",
    "sex_encoded", "cp_encoded", "fbs_encoded",
    "restecg_encoded", "exang_encoded", "slope_encoded", "thal_encoded"
  ],
  "numeric_features": ["age", "trestbps", "chol", "oldpeak"],
  "categorical_features": ["sex", "cp", "fbs", "restecg", "exang", "slope", "thal"],
  "target_variable": "thalach",
  "model_type": "LinearRegression",
  "train_r2": 0.3851,
  "test_r2": 0.1884,
  "train_rmse": 18.3000,
  "test_rmse": 16.7103,
  "train_mae": 14.3672,
  "test_mae": 13.8721,
  "intercept": 149.9375,
  "coefficients": {
    "age": -8.9959,
    "trestbps": 2.0489,
    "chol": 1.6092,
    "oldpeak": -1.5320,
    "sex_encoded": -0.4162,
    "cp_encoded": -0.7166,
    "fbs_encoded": 0.6052,
    "restecg_encoded": -0.1385,
    "exang_encoded": -6.4732,
    "slope_encoded": 5.3090,
    "thal_encoded": 0.1681
  },
  "most_important_feature": "age",
  "train_size": 240,
  "test_size": 61,
  "feature_means": [...],
  "feature_stds": [...]
}
```

---

### 🧪 tests/test_predict.py
**Unit тесты для модели**

**Класс TestRegressionModel (11 тестов):**
- `test_model_loaded_successfully` - Модель загружена
- `test_model_has_correct_config` - Config содержит нужные поля
- `test_prediction_output_format` - Формат предсказания
- `test_prediction_in_reasonable_range` - Диапазон предсказаний (60-220 bpm)
- `test_prediction_varies_with_age` - Изменение при разном age
- `test_get_model_info` - get_model_info() работает
- `test_get_r2_score` - get_r2_score() возвращает float в [0, 1]
- `test_get_equation_latex` - LaTeX уравнение генерируется
- `test_prepare_features` - Подготовка признаков работает
- `test_reproducibility` - Одинаковый вход → одинаковый выход
- `test_categorical_encoding` - Разные категории кодируются по-разному

**Класс TestModelConsistency (4 теста):**
- `test_feature_columns_match_config` - Признаки матчат конфиг
- `test_numeric_features_in_features` - Числовые признаки в списке
- `test_categorical_features_encoded` - Категориальные кодируются
- `test_coefficients_match_features` - Коэффициентов столько же сколько признаков

**Запуск:**
```bash
# Все тесты
python -m unittest tests.test_predict -v

# Конкретный класс
python -m unittest tests.test_predict.TestRegressionModel -v

# Конкретный тест
python -m unittest tests.test_predict.TestRegressionModel.test_prediction_output_format
```

**Результат:** ✅ 15/15 тестов проходят

---

### 📚 train_regression_model.py
**Скрипт для обучения модели**

Выполняет:
1. Загрузку данных из `cleve.mod`
2. Подготовку и кодирование признаков
3. Удаление выбросов (IQR метод)
4. Разделение на train/test (80/20)
5. Масштабирование признаков (StandardScaler)
6. Обучение LinearRegression модели
7. Сохранение модели, scaler, конфигурации

**Использование:**
```bash
python train_regression_model.py
```

Сохраняет:
- `models/regression_model.joblib`
- `models/regression_scaler.joblib`
- `models/regression_config.json`

---

### 📄 cleve.mod
**Исходный датасет Heart Disease Cleveland**

Формат:
- Разделитель: пробельные символы
- Комментарии: строки начинающиеся на '%'
- 303 пациента × 15 столбцов

**Столбцы:**
```
age, sex, cp, trestbps, chol, fbs, restecg, thalach, exang, oldpeak, slope, ca, thal, class, extra
```

**Используется:**
- Обучение моделей
- Загрузка в приложение для визуализации

---

### 📋 requirements.txt
**Зависимости Python**

```
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
gradio>=4.0.0
matplotlib>=3.7.0
```

**Установка:**
```bash
pip install -r requirements.txt
```

---

### 📖 README.md
**Основная документация проекта**

Содержит:
- Описание проекта и датасета
- Инструкции по установке
- Инструкции по запуску
- Структура проекта
- Python API примеры
- Информацию о модели
- Примеры входных данных
- Инструкции по тестированию
- Форматы файлов

---

### 🚀 USAGE_GUIDE.md
**Подробное руководство использования**

Содержит:
- Чек-лист выполнения требований
- Быстрый старт
- Параметры модели (таблица)
- Структура проекта (дерево)
- Использование (Web + Python API)
- Примеры входных данных
- Информацию о переобучении
- Техническую информацию
- Ограничения и улучшения

---

### ✅ COMPLETION_CHECKLIST.md
**Чек-лист выполнения всех требований**

Отслеживает:
- ✅ Все 7 обязательных требований
- ✅ 3 бонусных требования
- ✅ Наличие всех файлов
- ✅ Результаты тестов

---

## 🔄 Поток данных в приложении

```
Пользователь заполняет форму в браузере
        ↓
Gradio отправляет данные функции predict_and_plot()
        ↓
app.py вызывает model.predict()
        ↓
model.py кодирует категориальные признаки
        ↓
model.py масштабирует признаки (scaler.transform)
        ↓
regression_model.joblib делает предсказание
        ↓
app.py генерирует scatter plot
        ↓
app.py получает R² и LaTeX уравнение из model.get_*()
        ↓
Результаты выводятся в браузер:
  - Число (предсказание в bpm)
  - Изображение (scatter plot)
  - Markdown (R², уравнение, метрики)
```

---

## 💾 Размеры файлов

| Файл | Размер | Тип |
|------|--------|-----|
| regression_model.joblib | ~0.7 KB | Бинарный |
| regression_scaler.joblib | ~1.2 KB | Бинарный |
| regression_config.json | ~1.9 KB | JSON |
| app.py | ~6 KB | Python |
| model.py | ~4 KB | Python |
| test_predict.py | ~8 KB | Python |
| cleve.mod | ~24 KB | Text |
| requirements.txt | <1 KB | Text |
| README.md | ~8 KB | Markdown |

---

## 🔐 Загрузка и безопасность модели

**joblib vs pickle:**
- ✅ `joblib.dump/load` используется (более безопасен, лучше для больших объектов)
- ✅ Модель загружается один раз при старте приложения
- ✅ Сохраняется в `models/` директории

**Загрузка модели:**
```python
import joblib

model = joblib.load('models/regression_model.joblib')
scaler = joblib.load('models/regression_scaler.joblib')

# Использование
X_scaled = scaler.transform(X)
predictions = model.predict(X_scaled)
```

---

## 🎓 Как работает функция predict()

```python
def predict(age, trestbps, chol, oldpeak, sex, cp, fbs, restecg, exang, slope, thal):
    # 1. Подготовка признаков
    features = _prepare_features(...)  # Кодирование категориальных
    
    # 2. Масштабирование
    features_scaled = scaler.transform([features])
    
    # 3. Предсказание
    prediction = model.predict(features_scaled)[0]
    
    # 4. Возврат результата
    return {'prediction': prediction, 'features_dict': {...}}
```

---

## 📊 Примеры работы

### Пример 1: Молодой пациент
```
Input:  age=30, trestbps=110, chol=180, oldpeak=0.5, ...
Output: ~175 bpm (высокий макс. пульс)
```

### Пример 2: Пожилой пациент
```
Input:  age=70, trestbps=140, chol=260, oldpeak=2.5, ...
Output: ~125 bpm (низкий макс. пульс)
```

### Пример 3: Средний возраст
```
Input:  age=55, trestbps=120, chol=200, oldpeak=1.5, ...
Output: ~150 bpm (средний макс. пульс)
```

---

## ✅ Всё готово к использованию!

Проект полностью структурирован, протестирован и готов к production использованию.

Все файлы на месте ✅
Все тесты проходят ✅
Документация полная ✅
Приложение запущено ✅

